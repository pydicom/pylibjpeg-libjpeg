comment: false

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0.1%
    patch:
      default:
        target: auto
        threshold: 0.1%

ignore:
  - "libjpeg/tests"
  - "libjpeg/src"
